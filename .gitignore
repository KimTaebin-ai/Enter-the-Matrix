# 빌드 결과물 (가장 용량을 많이 차지함)
/target/

# Cargo.lock은 라이브러리라면 제외, 최종 실행 앱(Binary)이라면 포함하는 것이 관례입니다.
# 이 모듈은 라이브러리 성격이 강하므로 제외해도 무방합니다.
Cargo.lock

# macOS 사용자라면 생성되는 시스템 파일
.DS_Store

# 에디터 관련 설정 (개인 취향이므로 공유하지 않는 것이 좋음)
.vscode/
.idea/
*.swp